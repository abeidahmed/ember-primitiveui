{
  "private": "true",
  "workspaces": [
    "addon",
    "docs",
    "test-app"
  ],
  "version": "0.0.0",
  "scripts": {
    "lint": "yarn workspaces run lint",
    "test": "yarn workspace docs run test:ember && yarn workspace test-app run test:ember"
  },
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {}
}
