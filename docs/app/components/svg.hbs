{{svg-jar
  @icon
  height=(if @size @size "20")
  width=(if @size @size "20")
  fill="currentColor"
  role=(if @label "img" false)
  aria-label=(if @label @label false)
  aria-hidden=(if @label "false" "true")
}}
